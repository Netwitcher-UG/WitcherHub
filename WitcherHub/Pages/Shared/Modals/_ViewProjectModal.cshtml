<div class="modal fade" id="ViewProjectModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable modal-fullscreen-md-down">
        <div class="modal-content rounded-4">
            <div class="modal-header">
                <div class="w-100">
                    <div class="d-flex align-items-center justify-content-between">
                        <h5 class="modal-title mb-0" id="vp-title">Project</h5>
                        <span id="vp-statusBadge"></span>
                    </div>
                    <div class="text-muted small mt-1" id="vp-meta"></div>
                </div>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>

            <div class="modal-body">
                <div id="vpLoading">Loading...</div>

                <div id="vpBody" class="d-none">

                    <!-- Customer summary -->
                    <div class="mb-3">
                        <div class="text-muted small">Customer</div>
                        <div class="fw-semibold" id="vp-customerName">—</div>
                        <div class="text-muted small" id="vp-customerEmail">—</div>
                    </div>

                    <!-- Tabs -->
                    <ul class="nav nav-tabs" id="vpTabs" role="tablist">
                        <li class="nav-item" role="presentation">
                            <button class="nav-link active"
                                    id="vp-tab-overview"
                                    data-bs-toggle="tab"
                                    data-bs-target="#vp-pane-overview"
                                    type="button"
                                    role="tab">
                                Overview
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link"
                                    id="vp-tab-quotes"
                                    data-bs-toggle="tab"
                                    data-bs-target="#vp-pane-quotes"
                                    type="button"
                                    role="tab">
                                Quotes
                            </button>
                        </li>
                        <li class="nav-item" role="presentation">
                            <button class="nav-link"
                                    id="vp-tab-invoices"
                                    data-bs-toggle="tab"
                                    data-bs-target="#vp-pane-invoices"
                                    type="button"
                                    role="tab">
                                Invoices
                            </button>
                        </li>

                        <li class="nav-item" role="presentation">
                            <button class="nav-link"
                                    id="vp-tab-contracts"
                                    data-bs-toggle="tab"
                                    data-bs-target="#vp-pane-contracts"
                                    type="button"
                                    role="tab">
                                Contracts
                            </button>
                        </li>

                    </ul>

                    <div class="tab-content pt-3">

                        <!-- ===================== Overview ===================== -->
                        <div class="tab-pane fade show active" id="vp-pane-overview" role="tabpanel">
                            <!-- (نفس محتوى overview عندك بدون تغيير) -->
                            <div class="row g-3">
                                <div class="col-12 col-lg-6">
                                    <div class="card border-0 shadow-sm rounded-4">
                                        <div class="card-body">
                                            <div class="d-flex align-items-center justify-content-between">
                                                <h6 class="mb-0">Basic</h6>
                                            </div>

                                            <!-- View -->
                                            <div id="vp-basicView" class="mt-3">
                                                <div class="mb-2">
                                                    <div class="text-muted small">Title</div>
                                                    <div class="fw-semibold" id="vp-v-title">—</div>
                                                </div>
                                                <div class="mb-2">
                                                    <div class="text-muted small">Description</div>
                                                    <div id="vp-v-desc">—</div>
                                                </div>
                                                <div class="mb-2">
                                                    <div class="text-muted small">Dates</div>
                                                    <div id="vp-v-dates">—</div>
                                                </div>
                                                <div class="d-flex gap-2 mt-3">
                                                    <button class="btn btn-outline-primary btn-sm" type="button" id="vpEditBasicBtn">Edit</button>
                                                    <button class="btn btn-outline-secondary btn-sm" type="button" data-bs-dismiss="modal">Close</button>
                                                </div>

                                                <div class="alert alert-info mt-3 mb-0">
                                                    Tip: You can start directly with a <strong>Contract</strong> or <strong>Invoice</strong> (no Quote needed).
                                                </div>
                                            </div>

                                            <!-- Edit -->
                                            <div id="vp-basicEdit" class="d-none mt-3">
                                                <div class="mb-2">
                                                    <label class="form-label">Title</label>
                                                    <input class="form-control" id="vp-e-title" />
                                                    <div class="text-danger small" id="err-vp-e-title"></div>
                                                </div>

                                                <div class="mb-2">
                                                    <label class="form-label">Description</label>
                                                    <textarea class="form-control" rows="3" id="vp-e-desc"></textarea>
                                                    <div class="text-danger small" id="err-vp-e-desc"></div>
                                                </div>

                                                <div class="row g-2">
                                                    <div class="col-6">
                                                        <label class="form-label">Start</label>
                                                        <input type="date" class="form-control" id="vp-e-start" />
                                                        <div class="text-danger small" id="err-vp-e-start"></div>
                                                    </div>
                                                    <div class="col-6">
                                                        <label class="form-label">End</label>
                                                        <input type="date" class="form-control" id="vp-e-end" />
                                                        <div class="text-danger small" id="err-vp-e-end"></div>
                                                    </div>
                                                </div>

                                                <div class="d-flex gap-2 mt-3">
                                                    <button class="btn btn-primary btn-sm" type="button" id="vpSaveBasicBtn">Save</button>
                                                    <button class="btn btn-outline-secondary btn-sm" type="button" id="vpCancelBasicBtn">Cancel</button>
                                                </div>
                                            </div>

                                        </div>
                                    </div>
                                </div>

                                <div class="col-12 col-lg-6">
                                    <div class="card border-0 shadow-sm rounded-4">
                                        <div class="card-body">
                                            <h6 class="mb-3">Notes</h6>
                                            <div id="vpNotes">—</div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- ===================== Quotes ===================== -->
                        <div class="tab-pane fade" id="vp-pane-quotes" role="tabpanel">
                            <div class="d-flex align-items-center justify-content-between mb-3 gap-2 flex-wrap">
                                <div class="input-group" style="max-width: 520px;">
                                    <input class="form-control" id="vp-quotes-q" placeholder="Search quotes..." />
                                    <button class="btn btn-outline-secondary" type="button" id="vp-quotes-searchBtn">Search</button>
                                </div>

                                <!-- ✅ Split button: keep vp-newQuoteBtn id so existing JS keeps working -->
                                <a class="btn btn-primary" href="#" id="vp-newQuoteBtn">
                                    + New Quote
                                </a>


                            </div>

                            <div id="vpQuotesLoading" class="text-muted small d-none">Loading quotes...</div>

                            <div id="vpQuotesEmpty" class="alert alert-light border d-none">
                                No quotes yet.
                            </div>

                            <div class="table-responsive">
                                <table class="table table-sm align-middle mb-2" id="vpQuotesTable">
                                    <thead>
                                        <tr>
                                            <th>Quote No</th>
                                            <th>Status</th>
                                            <th>Created</th>
                                            <th class="text-end">Total</th>
                                            <th class="text-end">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody id="vpQuotesTbody"></tbody>
                                </table>
                            </div>

                            <nav class="d-none" id="vpQuotesPagerWrap">
                                <ul class="pagination pagination-sm mb-0" id="vpQuotesPager"></ul>
                            </nav>
                        </div>
                        <!-- ===================== Invoices ===================== -->
                        <div class="tab-pane fade" id="vp-pane-invoices" role="tabpanel">

                            <div class="d-flex align-items-center justify-content-between mb-3 gap-2 flex-wrap">
                                <div class="input-group" style="max-width: 520px;">
                                    <input class="form-control" id="vp-invoices-q" placeholder="Search invoices..." />
                                    <button class="btn btn-outline-secondary" type="button" id="vp-invoices-searchBtn">Search</button>
                                </div>

                                <a class="btn btn-primary" href="#" id="vp-newInvoiceBtn2">+ New Invoice</a>
                            </div>

                            <div id="vpInvoicesLoading" class="text-muted small d-none">Loading invoices...</div>

                            <div id="vpInvoicesEmpty" class="alert alert-light border d-none">
                                No invoices yet.
                            </div>

                            <div class="table-responsive">
                                <table class="table table-sm align-middle mb-2 d-none" id="vpInvoicesTable">
                                    <thead>
                                        <tr>
                                            <th>Invoice No</th>
                                            <th>Status</th>
                                            <th>Created</th>
                                            <th>Issue</th>
                                            <th>Due</th>
                                            <th class="text-end">Total</th>
                                            <th class="text-end">Actions</th>
                                        </tr>
                                    </thead>
                                    <tbody id="vpInvoicesTbody"></tbody>
                                </table>
                            </div>

                            <nav class="d-none" id="vpInvoicesPagerWrap">
                                <ul class="pagination pagination-sm mb-0" id="vpInvoicesPager"></ul>
                            </nav>
                        </div>

                        <!-- ===================== Contracts ===================== -->
                        <div class="tab-pane fade" id="vp-pane-contracts" role="tabpanel">

                            <div class="d-flex align-items-center justify-content-between mb-3 gap-2 flex-wrap">
                                <div>
                                    <div class="text-muted small">Contract</div>
                                    <div class="fw-semibold" id="vpContractTitle">—</div>
                                    <div class="text-muted small" id="vpContractMeta">—</div>
                                </div>

                                <div class="d-flex gap-2 flex-wrap">
                                    <a class="btn btn-outline-primary d-none"
                                       id="vpContractItemsLink"
                                       href="#">
                                        Line Items
                                    </a>
                                    <!-- يظهر فقط إذا لا يوجد عقد -->
                                    <button class="btn btn-primary d-none"
                                            type="button"
                                            id="vpContractCreateBtn">
                                        Add Contract
                                    </button>

                                    <!-- يظهر فقط إذا يوجد عقد -->
                                    <button class="btn btn-primary d-none"
                                            type="button"
                                            id="vpContractGenerateBtn">
                                        Update Contract
                                    </button>

                                    <a class="btn btn-outline-secondary d-none"
                                       id="vpContractEditLink"
                                       href="#">Edit</a>

                                    <a class="btn btn-outline-primary d-none"
                                       id="vpContractDetailsLink"
                                       href="#">Details</a>

                                    <button class="btn btn-outline-success d-none"
                                            type="button"
                                            id="vpContractSendBtn"
                                            disabled>
                                        Send
                                    </button>
                                </div>

                            </div>

                            <div id="vpContractLoading" class="text-muted small d-none">Loading contract...</div>

                            <div id="vpContractEmpty" class="alert alert-light border d-none">
                                No contract yet for this project.
                            </div>

                            <div id="vpContractPreviewWrap" class="card border-0 shadow-sm rounded-4 d-none">
                                <div class="card-body">
                                    <div class="d-flex align-items-center justify-content-between mb-2">
                                        <h6 class="mb-0">Preview</h6>
                                        <span id="vpContractStatusBadge"></span>
                                    </div>

                                    <div id="vpContractPreview"
                                         style="max-height: 520px; overflow:auto; border:1px solid rgba(0,0,0,.08); border-radius:12px; padding:14px;">
                                    </div>
                                </div>
                            </div>

                        </div>


                    </div>
                </div>
            </div>

        </div>
    </div>
</div>

<script>
    (function () {
      const modalEl = document.getElementById('ViewProjectModal');
      if (!modalEl) return;

      modalEl.addEventListener('show.bs.modal', function (ev) {
        const trigger = ev.relatedTarget;

        const projectId = trigger?.getAttribute('data-project-id') || trigger?.dataset?.projectId || '';
        const openTab = trigger?.getAttribute('data-open-tab') || trigger?.dataset?.openTab || '';

        if (projectId) modalEl.dataset.projectId = projectId;
        if (openTab) modalEl.dataset.openTab = openTab;

        // Create links from current project
        const q = document.getElementById('vp-newQuoteBtn');
        if (q) q.href = projectId ? `/Quotes/Create?projectId=${encodeURIComponent(projectId)}` : '#';

        const c = document.getElementById('vp-newContractBtn');
        if (c) c.href = projectId ? `/Contracts/Create?projectId=${encodeURIComponent(projectId)}` : '#';

        const i = document.getElementById('vp-newInvoiceBtn');
        if (i) i.href = projectId ? `/Invoices/Create?projectId=${encodeURIComponent(projectId)}` : '#';
      });

      modalEl.addEventListener('shown.bs.modal', function () {
        const openTab = modalEl.dataset.openTab;
        if (!openTab) return;

        const btn = modalEl.querySelector(`#vpTabs button[data-bs-target="#vp-pane-${openTab}"]`);
        if (!btn) return;

        bootstrap.Tab.getOrCreateInstance(btn).show();
      });
    })();
</script>
