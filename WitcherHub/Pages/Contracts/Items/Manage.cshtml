@page
@model WitcherHub.Pages.Contracts.Items.ManageModel
@*
    Redirects to /Contracts/Positions/{contractId}. See the page model for why.
    Nothing renders here; the handler always returns a redirect.
*@
